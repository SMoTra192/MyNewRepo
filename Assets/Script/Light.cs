using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Light : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
     print(message:"Lightning");   
    }

    // Update is called once per frame
    void Update()
    {
    print(message:"Lightning");    
    }
}
